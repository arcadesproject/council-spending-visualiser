---
import { onMount } from 'astro/jsx-runtime';

onMount(async () => {
  const Chart = await import('chart.js/auto');
  const response = await fetch('/data/council_spending.json');
  const data = await response.json();

  // Chart logic here...
});
---

<canvas id="spendingChart"></canvas>
